# Normalize all text files to LF in the repo and on checkout. The repo had mixed
# CRLF/LF line endings with no policy, so a stray editor/tool round-trip could
# rewrite a whole file's endings and bury real changes in whitespace churn. Git
# auto-detects binary files and leaves them untouched.
* text=auto eol=lf
